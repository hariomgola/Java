package Java_8;

import java.util.List;

public class invesco_l2_Question {
/**
 * Create a React class component named `Person` that takes a `Person` object as a prop. 
 * The `Person` object should have three fields: `Name`, `Lastname`, and
 *  `Addresses` (which is a list of `Address` objects). 
 * Display the `Name` and `Lastname` in an `h1` tag and list all the addresses in an unordered list.
 */
	
/**

 const Person = ({Name,LastName,Address})=>{
     return(
     <>
        <h1>{Name}</h1>
        <h1>{LastName}</h1>
        <ul>{Address.map(_data=>{
          return <li></li>
        )}</ul>
     </>
     )
 }
 
 Set up a route in your main application to 
 navigate to a `Person` page Use `react-router-dom` for routing.
 
 <>
 isAuthernticated
 router="/person"
 </>
  
  // Set up a Redux store to manage the `Person` state. 
  // Create actions and reducers to handle the `Person`.
  
  - action.js
//  - facade.js
//  - effect.js
  - reducer.js
//  - selector.js
  
  getPersonService();
  
  personReducer
  const getPersonService=()=>{
    this.personReducer.getPersonSourceData();
  }
  cosnt getPerson$ = 
  
  
  getPerson$
  .pipe(takeuntil())
  .subscriber(_data->{
    
  })
  
  //
  // Modify the `PersonPage` component to retrieve the `Person` 
  // data from the Redux store and pass it to the `Person` component.
  ///
  // PersonPage
  //
  
  // person Page
  useEffect(()=>{
    getPerson$.subscribe(_data=>{
    
    })
  },[])
  
  let data: String 
  "1"+1 = 2
  "1"+1 = 11
 
  
  
 */
	
}
