let QuestionAsked = () => `

1. How to return any value from the Java Thread
- Use callable to return value

2. Difference BW Runnable and callable
- Runnable will simple run the function it will not return any value
- callable will return the value

3. Difference bw concurrent hasnmap and syncronized hashmap
- null key is alloed in synchronized hashmap where as its not allowed in concurrent hashmap
- S-map is slower instead of c-map is faster

4. What is hashcollision
- hash collision occurs when the 2 hashcode is equals
- when a collision occurs, Java stores the colliding entries in a linked list.
`;
